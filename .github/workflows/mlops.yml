name: MLOps Pipeline

on:
  workflow_dispatch:    # only run when one manually triggers
#   push:
#     branches: [main]

jobs:
  train-and-register:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.10

      - name: Install dependencies
        run: pip install -r requirements.txt

      # - name: Train model
      #   run: python train.py

      # - name: Convert to ONNX
      #   run: python convert_onnx.py

      # - name: Build API image
      #   run: docker build -t sentiment-api ./api

      # - name: Push Docker image
      #   run: |
      #     docker tag sentiment-api your-dockerhub/sentiment-api
      #     docker push your-dockerhub/sentiment-api